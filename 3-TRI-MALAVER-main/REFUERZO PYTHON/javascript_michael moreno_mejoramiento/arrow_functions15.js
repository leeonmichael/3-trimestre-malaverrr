const michael_moreno1 = (cuervoBase, cuervoAltura) => {
  return (cuervoBase * cuervoAltura) / 2;
};

const michael_moreno2 = (cuervoBase, cuervoAltura) =>
  (cuervoBase * cuervoAltura) / 2;

console.log("Ãrea:", michael_moreno1(8, 5));

// TIP: Puedes usar funciones flecha con return implÃ­cito para simplificar cÃ¡lculos como este.
